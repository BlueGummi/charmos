#! expect: tasks[0].boot.stat_interval_ms
#! because: heartbeat too slow 
[suite]
name = "no_pulse"
runners = 1
budget_hours = 1.0

[build]

[[tasks]]
name = "t"
  [tasks.boot]
  duration_ms      = 10000
  stat_interval_ms = 30000
