# Authoritative planner fixtures

These are stable P8 planning inputs. They cover accepted placement, duplicate
display names, stale snapshots, unknown suites, contiguous-capacity rejection,
and bounded materialization. Planner tests inject the evaluation clock and
source identity; no fixture launches QEMU.
