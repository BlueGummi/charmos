This repository is a hobby operating system kernel written in C.

Guidelines:
- Never invent functions that are not defined in the repository
- Follow the kernel coding style
- Avoid dynamic allocation in hot paths
- Use kmalloc/kfree instead of malloc/free
